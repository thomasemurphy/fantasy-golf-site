<%# === NAV ROW 1: season-level tabs === %>
<ul class="nav nav-pills mb-2" id="mainTabs" role="tablist">
  <% if @live_tournament %>
    <li class="nav-item" role="presentation">
      <button class="nav-link <%= 'active' if @initial_tab == 'live' %>"
              data-bs-toggle="tab" data-bs-target="#tab-live" type="button" role="tab">
        <span class="text-danger fw-bold">&#9679;</span> Live
      </button>
    </li>
  <% end %>
  <% [["overall","Main Pot","tab-label-regular","~60% of total pot, ~$6,700","All events"],["majors","Majors Pot","tab-label-major","15% of total pot, ~$1,600",nil],["side_events","High-Purse Side Pot","tab-label-side-event","15% of total pot, ~$1,600",nil],["pink_events","Low-Purse Side Pot","tab-label-pink-event","$500",nil],["first_half","🕐 1st Half","tab-label-first-half","$300",nil],["second_half","🕢 2nd Half","tab-label-second-half","$300",nil]].each do |tid, label, css_class, prize_text, body_text| %>
    <% pot_tourneys = @pot_tournaments[tid] %>
    <li class="nav-item" role="presentation">
      <button class="nav-link <%= 'active' if @initial_tab == tid %>"
              data-bs-toggle="tab" data-bs-target="#tab-<%= tid %>" type="button" role="tab">
        <% if prize_text %>
          <span data-controller="tooltip" data-tooltip-position-value="below">
            <span data-tooltip-target="trigger"
                  data-action="mouseenter->tooltip#show mouseleave->tooltip#hide"
                  class="<%= css_class %>"><%= label %></span>
            <span data-tooltip-target="content" style="display:none">
              <div style="color:#aaa;font-style:italic;margin-bottom:4px"><%= prize_text %></div>
              <% if pot_tourneys %>
                <table style="border-spacing:0;line-height:1.9">
                  <% pot_tourneys.each do |t| %>
                    <% d1 = t.start_date; d2 = t.end_date %>
                    <% dates = d1.month == d2.month ? "#{d1.strftime('%b %-d')}-#{d2.strftime('%-d')}" : "#{d1.strftime('%b %-d')}-#{d2.strftime('%b %-d')}" %>
                    <tr>
                      <td style="color:#aaa;padding-right:10px;white-space:nowrap">Wk&nbsp;<%= t.week_number %></td>
                      <td style="padding-right:12px;white-space:nowrap"><%= short_tournament_name(t.name) %></td>
                      <td style="white-space:nowrap;color:#6c757d"><%= dates %></td>
                    </tr>
                  <% end %>
                </table>
              <% elsif body_text %>
                <div style="color:#6c757d;margin-top:2px"><%= body_text %></div>
              <% end %>
            </span>
          </span>
        <% elsif css_class %>
          <span class="<%= css_class %>"><%= label %></span>
        <% else %>
          <%= label %>
        <% end %>
      </button>
    </li>
  <% end %>
</ul>

<%# === NAV ROW 2: per-tournament week tabs === %>
<% if @completed_tournaments.any? || @upcoming_tournaments.any? %>
  <% type_class = { "regular" => "tab-label-regular", "major" => "tab-label-major", "side_event" => "tab-label-side-event", "pink_event" => "tab-label-pink-event" } %>
  <div id="week-tabs-wrapper" class="mb-4">
    <%# Header row: toggle + (in collapsed state) either the live tab or "Tournaments" label %>
    <div id="week-tabs-header" class="d-flex align-items-center gap-2 flex-wrap">
      <button type="button" id="week-tabs-toggle"
              class="btn btn-sm border-0 p-0 text-secondary"
              style="font-size:0.82rem; line-height:1.4; background:none">
        <span id="week-tabs-chevron">&#9654;</span> Tournaments
      </button>
    </div>
    <%# Full tab list — shown when expanded %>
    <ul class="nav nav-pills nav-pills-sm flex-wrap mt-1" id="weekTabs" role="tablist">
      <% (@completed_tournaments.to_a + @upcoming_tournaments.to_a).each do |t| %>
        <% upcoming = t.status == "upcoming" %>
        <% target     = t.status == "in_progress" ? "tab-live" : "tab-t#{t.id}" %>
        <% active     = t.status == "in_progress" ? @initial_tab == "live" : @initial_tab == "t#{t.id}" %>
        <% name_class = type_class[t.tournament_type] %>
        <% short      = short_tournament_name(t.name) %>
        <% winner     = @tournament_winners&.dig(t.id) %>
        <% has_venue  = t.course_name.present? %>
        <li class="nav-item" role="presentation">
          <% if has_venue || winner %>
            <span data-controller="tooltip" data-tooltip-position-value="below" data-tooltip-title-value="<%= t.name %>">
              <% if upcoming %>
                <span class="nav-link-sm d-inline-block" style="opacity:0.38;cursor:default"
                      data-tooltip-target="trigger"
                      data-action="mouseenter->tooltip#show mouseleave->tooltip#hide">
                  <span class="text-muted fw-normal me-1" style="font-size:0.7em">Wk&nbsp;<%= t.week_number %></span><% if name_class %><span class="<%= name_class %>"><%= short %></span><% else %><%= short %><% end %>
                </span>
              <% else %>
                <button class="nav-link nav-link-sm <%= 'active' if active %>"
                        data-bs-toggle="tab" data-bs-target="#<%= target %>" type="button" role="tab"
                        data-tooltip-target="trigger"
                        data-action="mouseenter->tooltip#show mouseleave->tooltip#hide click->tooltip#hide">
                  <span class="text-muted fw-normal me-1" style="font-size:0.7em">Wk&nbsp;<%= t.week_number %></span><% if name_class %><span class="<%= name_class %>"><%= short %></span><% else %><%= short %><% end %>
                </button>
              <% end %>
              <span data-tooltip-target="content" style="display:none">
                <div style="white-space:nowrap">
                  <% if t.course_name.present? %>
                    <div style="margin-bottom:2px"><%= t.course_name %></div>
                    <div style="color:#aaa;margin-bottom:4px"><%= [t.city, t.state].compact.join(", ") %></div>
                  <% end %>
                  <% if (date_range = tournament_date_range(t)) %>
                    <div style="color:#6c757d;margin-bottom:<%= (t.purse_cents.to_i > 0 || winner) ? '4px' : '0' %>"><%= date_range %></div>
                  <% end %>
                  <% if t.purse_cents.to_i > 0 %>
                    <div style="color:#6c757d;margin-bottom:<%= winner ? '4px' : '0' %>">Purse: $<%= number_with_delimiter((t.purse_cents / 100.0).to_i) %></div>
                  <% end %>
                  <% if winner %>
                    <div style="color:#6c757d">Winner: <%= winner %></div>
                  <% end %>
                </div>
              </span>
            </span>
          <% elsif upcoming %>
            <span class="nav-link-sm d-inline-block" style="opacity:0.38;cursor:default">
              <span class="text-muted fw-normal me-1" style="font-size:0.7em">Wk&nbsp;<%= t.week_number %></span><% if name_class %><span class="<%= name_class %>"><%= short %></span><% else %><%= short %><% end %>
            </span>
          <% else %>
            <button class="nav-link nav-link-sm <%= 'active' if active %>"
                    data-bs-toggle="tab" data-bs-target="#<%= target %>" type="button" role="tab">
              <span class="text-muted fw-normal me-1" style="font-size:0.7em">Wk&nbsp;<%= t.week_number %></span><% if name_class %><span class="<%= name_class %>"><%= short %></span><% else %><%= short %><% end %>
            </button>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>

<%# === ALL TAB CONTENT === %>
<div class="tab-content">

  <%# --- Live tab --- %>
  <% if @live_tournament %>
    <%= render "live_tab_frame" %>
  <% end %>

  <%# --- Season standings tabs (Overall, Majors, Side Events, 1st Half, 2nd Half) --- %>

  <%# Overall tab has a Pool Players / All Golfers toggle %>
  <div id="tab-overall" class="tab-pane fade <%= 'show active' if @initial_tab == 'overall' %>" role="tabpanel">
    <div class="standings-table-wrapper">
      <%# Toggle header inside the card %>
      <div class="d-flex justify-content-end p-2 border-bottom">
        <div class="btn-group btn-group-sm pool-field-toggle" role="group">
          <button class="btn btn-outline-secondary active"
                  data-bs-toggle="tab" data-bs-target="#overall-pool" type="button">Pool Players</button>
          <button class="btn btn-outline-secondary"
                  data-bs-toggle="tab" data-bs-target="#overall-golfers" type="button">All Golfers</button>
        </div>
      </div>
      <div class="tab-content">
        <div id="overall-pool" class="tab-pane show active">
          <%= render "season_table", standings: @overall_standings, tab_id: "overall", skip_wrapper: true %>
        </div>
        <div id="overall-golfers" class="tab-pane">
          <table class="standings-table">
            <thead>
              <tr>
                <th>Rank</th>
                <th>Golfer</th>
                <th class="text-end">Season Earnings</th>
              </tr>
            </thead>
            <tbody>
              <% @all_season_golfers.each do |row| %>
                <tr>
                  <td><span class="standings-rank"><%= row[:rank] %></span></td>
                  <td>
                    <% gh = row[:history] %>
                    <% if gh.any? { |e| e[:result] } %>
                      <% total_earnings = row[:earnings_cents] %>
                      <span data-controller="tooltip"
                            data-tooltip-title-value="<%= row[:golfer].name %>"
                            <% if total_earnings > 0 %>data-tooltip-earnings-value="$<%= number_with_delimiter((total_earnings / 100.0).to_i) %>"<% end %>>
                        <span data-tooltip-target="trigger"
                              data-action="mouseenter->tooltip#show mouseleave->tooltip#hide"
                              class="pick-history-name"><%= row[:golfer].name %></span>
                        <span data-tooltip-target="content" style="display:none">
                          <table style="border-spacing:0;line-height:1.9">
                            <% gh.each do |entry| %>
                              <% res = entry[:result] %>
                              <tr>
                                <td style="color:#aaa;padding-right:10px;white-space:nowrap">Wk<%= entry[:tournament].week_number %></td>
                                <td style="padding-right:12px;white-space:nowrap"><%= short_tournament_name(entry[:tournament].name) %></td>
                                <% if res %>
                                  <td style="padding-right:12px;white-space:nowrap;color:#6c757d"><%= res.current_position_display.presence || "—" %></td>
                                  <td style="text-align:right;white-space:nowrap">
                                    <% if res.earnings_cents.to_i > 0 %>
                                      $<%= number_with_delimiter((res.earnings_cents / 100.0).to_i) %>
                                    <% else %>
                                      <span style="color:#aaa">$0</span>
                                    <% end %>
                                  </td>
                                <% else %>
                                  <td style="padding-right:12px;white-space:nowrap;color:#aaa">—</td>
                                  <td style="text-align:right;white-space:nowrap;color:#aaa">—</td>
                                <% end %>
                              </tr>
                            <% end %>
                          </table>
                        </span>
                      </span>
                    <% else %>
                      <%= row[:golfer].name %>
                    <% end %>
                  </td>
                  <td class="text-end tabular-nums">
                    <% if row[:earnings_cents] > 0 %>
                      $<%= number_with_delimiter((row[:earnings_cents] / 100.0).to_i) %>
                    <% else %>
                      <span class="text-muted">$0</span>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <% if @live_tournament %>
    <%# Season sub-tabs: deferred via Turbo Frame, loaded on first click %>
    <% %w[majors side_events pink_events first_half second_half].each do |tid| %>
      <turbo-frame id="tab-<%= tid %>"
                   class="tab-pane fade <%= 'show active' if @initial_tab == tid %>"
                   role="tabpanel"
                   data-src="<%= tab_standings_path(tab: tid) %>">
        <div class="text-center py-4 text-muted" style="font-size:0.9em">Loading...</div>
      </turbo-frame>
    <% end %>

    <%# Per-tournament tabs: deferred via Turbo Frame %>
    <% @completed_tournaments.reject { |t| t.status == "in_progress" }.each do |t| %>
      <turbo-frame id="tab-t<%= t.id %>"
                   class="tab-pane fade <%= 'show active' if @initial_tab == "t#{t.id}" %>"
                   role="tabpanel"
                   data-src="<%= tab_standings_path(tab: "t#{t.id}") %>">
        <div class="text-center py-4 text-muted" style="font-size:0.9em">Loading...</div>
      </turbo-frame>
    <% end %>
  <% else %>
    <%# Season sub-tabs: render initial tab inline, defer all others via Turbo Frame %>
    <% %w[majors side_events pink_events first_half second_half].each do |tid| %>
      <% if @initial_tab == tid %>
        <div id="tab-<%= tid %>" class="tab-pane fade show active" role="tabpanel">
          <%= render "season_table", standings: @active_tab_standings, tab_id: tid %>
        </div>
      <% else %>
        <turbo-frame id="tab-<%= tid %>"
                     class="tab-pane fade"
                     role="tabpanel"
                     data-src="<%= tab_standings_path(tab: tid) %>">
          <div class="text-center py-4 text-muted" style="font-size:0.9em">Loading...</div>
        </turbo-frame>
      <% end %>
    <% end %>

    <%# Per-tournament tabs: render initial tab inline, defer all others via Turbo Frame %>
    <% @completed_tournaments.reject { |t| t.status == "in_progress" }.each do |t| %>
      <% tab_id = "t#{t.id}" %>
      <% if @initial_tab == tab_id %>
        <%= render "tournament_pane", td: @active_tournament_data %>
      <% else %>
        <turbo-frame id="tab-<%= tab_id %>"
                     class="tab-pane fade"
                     role="tabpanel"
                     data-src="<%= tab_standings_path(tab: tab_id) %>">
          <div class="text-center py-4 text-muted" style="font-size:0.9em">Loading...</div>
        </turbo-frame>
      <% end %>
    <% end %>
  <% end %>

</div><%# .tab-content %>

<%# Sync active state across the two nav rows when switching tabs %>
<script>
  document.addEventListener('turbo:load', function() {
    var outerTabContent = document.querySelector('div.tab-content');
    var allNavBtns = document.querySelectorAll('#mainTabs [data-bs-toggle="tab"], #weekTabs [data-bs-toggle="tab"]');

    allNavBtns.forEach(function(btn) {
      // Bootstrap only searches within the same <ul> for the active item, so cross-nav
      // switching doesn't auto-deactivate the other nav's pane. Manually clear it first.
      btn.addEventListener('show.bs.tab', function() {
        outerTabContent.querySelectorAll(':scope > .tab-pane.active').forEach(function(pane) {
          pane.classList.remove('show', 'active');
        });
      });

      btn.addEventListener('shown.bs.tab', function() {
        allNavBtns.forEach(function(b) { if (b !== btn) b.classList.remove('active'); });

        // Trigger lazy load for deferred turbo-frame tabs on first click
        var pane = outerTabContent.querySelector(btn.getAttribute('data-bs-target'));
        if (pane && pane.tagName === 'TURBO-FRAME' && pane.dataset.src && !pane.getAttribute('src')) {
          pane.src = pane.dataset.src;
        }

        // Update URL so a page refresh returns to this tab
        var tabId = (btn.getAttribute('data-bs-target') || '').replace(/^#tab-/, '');
        if (tabId) {
          var url = new URL(window.location.href);
          url.searchParams.set('tab', tabId);
          url.searchParams.delete('tournament_id');
          // Clear field_view when leaving a tournament tab
          if (!tabId.match(/^t\d+$/)) url.searchParams.delete('field_view');
          history.replaceState({}, '', url.pathname + url.search);
        }
      });
    });

    // Also trigger for any deferred turbo-frame tab that's active on initial page load
    // (e.g. user bookmarked /standings?tab=majors during a live tournament)
    outerTabContent.querySelectorAll(':scope > turbo-frame[data-src]').forEach(function(frame) {
      if (frame.classList.contains('show') && frame.classList.contains('active') && !frame.getAttribute('src')) {
        frame.src = frame.getAttribute('data-src');
      }
    });

    // Restore All Golfers sub-tab when field_view=1 is in the URL
    restoreFieldView();
  });

  // Also restore after a Turbo Frame finishes loading (lazy-loaded tournament tab)
  document.addEventListener('turbo:frame-load', function(e) {
    if (/^tab-t\d+$/.test(e.target.id)) restoreFieldView();
  });

  function restoreFieldView() {
    if (new URLSearchParams(window.location.search).get('field_view') !== '1') return;
    var outerTabContent = document.querySelector('div.tab-content');
    if (!outerTabContent) return;
    var activeTourneyPane = outerTabContent.querySelector(':scope > .show.active[id^="tab-t"]');
    if (!activeTourneyPane) return;
    var poolBtn  = activeTourneyPane.querySelector('.pool-field-toggle [data-bs-target$="-pool"]');
    var fieldBtn = activeTourneyPane.querySelector('.pool-field-toggle [data-bs-target$="-field"]');
    if (!fieldBtn) return;
    var poolPane  = poolBtn  ? activeTourneyPane.querySelector(poolBtn.getAttribute('data-bs-target'))  : null;
    var fieldPane = activeTourneyPane.querySelector(fieldBtn.getAttribute('data-bs-target'));
    if (!fieldPane) return;
    if (poolPane)  { poolPane.classList.remove('show', 'active');  }
    if (poolBtn)   { poolBtn.classList.remove('active');           }
    fieldPane.classList.add('show', 'active');
    fieldBtn.classList.add('active');
  }

  // Pool/Field toggle: use event delegation so buttons inside lazy-loaded Turbo Frames
  // are handled even though they don't exist at turbo:load time.
  document.addEventListener('click', function(e) {
      var btn = e.target.closest('.pool-field-toggle [data-bs-toggle="tab"]');
      if (!btn) return;

      // Keep live_sub in the refresh link href in sync
      var refreshBtn = document.getElementById("refresh-btn");
      var liveTarget = btn.getAttribute("data-bs-target") || "";
      var liveSub = liveTarget === "#live-field" ? "field" : "pool";
      if (refreshBtn) {
        var url = new URL(refreshBtn.getAttribute("href"), window.location.href);
        url.searchParams.set("live_sub", liveSub);
        refreshBtn.setAttribute("href", url.pathname + url.search);
      }

      // Update URL so a page refresh returns to this pool/field sub-tab
      if (liveTarget.startsWith("#live-")) {
        var pageUrl = new URL(window.location.href);
        pageUrl.searchParams.set("live_sub", liveSub);
        history.replaceState({}, '', pageUrl.pathname + pageUrl.search);
      }

      // Track All Golfers / Pool Players within completed tournament tabs
      if (liveTarget.match(/^#t\d+-/)) {
        var pageUrl2 = new URL(window.location.href);
        if (liveTarget.endsWith("-field")) {
          pageUrl2.searchParams.set("field_view", "1");
        } else {
          pageUrl2.searchParams.delete("field_view");
        }
        history.replaceState({}, '', pageUrl2.pathname + pageUrl2.search);
      }

      // Handle switching ourselves so we don't depend on Bootstrap finding the active pane
      // (it can't, because these buttons are in .btn-group not .nav).
      var target = document.querySelector(btn.getAttribute('data-bs-target'));
      if (!target) return;
      // Deactivate all sibling panes
      target.parentElement.querySelectorAll(':scope > .tab-pane').forEach(function(pane) {
        pane.classList.remove('show', 'active');
      });
      // Activate the clicked pane
      target.classList.add('show', 'active');
      // Sync button states
      var group = btn.closest('.pool-field-toggle');
      if (group) {
        group.querySelectorAll('[data-bs-toggle="tab"]').forEach(function(b) {
          b.classList.toggle('active', b === btn);
        });
      }
  });
</script>

<% if @last_refreshed %>
<script>
  (function() {
    var refreshedAt = <%= @last_refreshed.to_i %> * 1000;
    var cooldown = <%= StandingsController::REFRESH_COOLDOWN %>;
    function updateLabels() {
      var elapsed = (Date.now() - refreshedAt) / 1000;
      var text;
      if (elapsed < 8) {
        text = "Updated just now";
      } else if (elapsed < 60) {
        text = "Updated less than a minute ago";
      } else {
        var mins = Math.floor(elapsed / 60);
        text = "Updated " + mins + (mins === 1 ? " minute" : " minutes") + " ago";
      }
      document.querySelectorAll(".refreshed-label").forEach(function(el) { el.textContent = text; });

      // Re-enable the refresh button once cooldown has passed
      var btn = document.getElementById("refresh-btn");
      if (btn && elapsed >= cooldown) {
        btn.classList.remove("disabled");
      }
    }
    updateLabels();
    setInterval(updateLabels, 1000);
  })();
</script>
<% end %>

<script>
  (function() {
    // Keep return_tab in the refresh link href in sync with the active tab
    function syncReturnTab() {
      var btn = document.getElementById("refresh-btn");
      if (!btn) return;
      var active = document.querySelector(".nav-tabs .nav-link.active");
      if (active && active.dataset.bsTarget) {
        var url = new URL(btn.getAttribute("href"), window.location.href);
        url.searchParams.set("return_tab", active.dataset.bsTarget.replace("#tab-", ""));
        btn.setAttribute("href", url.pathname + url.search);
      }
    }
    document.addEventListener("turbo:load", function() {
      syncReturnTab();
      document.querySelectorAll(".nav-tabs .nav-link").forEach(function(tab) {
        tab.addEventListener("shown.bs.tab", syncReturnTab);
      });
    });
  })();
</script>

<script>
  (function() {
    var toggle  = document.getElementById("week-tabs-toggle");
    var list    = document.getElementById("weekTabs");
    var chevron = document.getElementById("week-tabs-chevron");
    if (!toggle || !list) return;

    function applyState(expanded) {
      list.style.display = expanded ? "" : "none";
      chevron.innerHTML = expanded ? "&#9660;" : "&#9654;";
    }

    var stored = localStorage.getItem("weekTabsExpanded");
    // Default to expanded, but collapse if user has previously collapsed
    var expanded = stored === null ? true : stored === "true";
    applyState(expanded);

    toggle.addEventListener("click", function() {
      expanded = !expanded;
      localStorage.setItem("weekTabsExpanded", expanded);
      applyState(expanded);
    });
  })();
</script>
